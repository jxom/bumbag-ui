---
name: Tooltip
route: /components/tooltip
menu: Components
---

import { Playground } from 'docz';
import PropsTable from '../_docs/components/PropsTable';
import { Box } from '../primitives';
import Button from '../Button';
import {Tooltip} from './Tooltip';

# Tooltip

## Import

`import { Tooltip } from 'fannypack'`

## Basic Usage

<Playground>
  <Box height="50px">
    <Button>
      Hover on me
      <Tooltip>Hello world!</Tooltip>
    </Button>
  </Box>
</Playground>

## Placement

You can place the tooltip to a particular side of the target with the `placement` prop.

Available values are: `auto`, `top`, `right`, `bottom`, `left`, `top-start`, `right-start`, `bottom-start`, `left-start`, `top-end`, `right-end`, `bottom-end`, `left-end`.

<Playground>
  <Button>
    Hover on me
    <Tooltip placement="right">Hello world!</Tooltip>
  </Button>
</Playground>

## Animations

### Fade

<Playground>
  <Box height="50px">
    <Button>
      Hover on me
      <Tooltip fade>Hello world!</Tooltip>
    </Button>
  </Box>
</Playground>

### Slide

<Playground>
  <Box height="50px">
    <Button>
      Hover on me
      <Tooltip slide fade>Hello world!</Tooltip>
    </Button>
  </Box>
</Playground>

### Expand

<Playground>
  <Box height="50px">
    <Button>
      Hover on me
      <Tooltip expand fade slide>Hello world!</Tooltip>
    </Button>
  </Box>
</Playground>

## `<Tooltip>` Props

<PropsTable component={Tooltip} />

## Theming

### `<Tooltip>` Schema

```jsx
{
  base: string | Object;
}
```
