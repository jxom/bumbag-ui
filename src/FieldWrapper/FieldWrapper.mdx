---
name: FieldWrapper
route: /form/fieldwrapper
menu: Form
---

import { Playground } from 'docz';
import PropsTable from '../_docs/components/PropsTable';
import { Box } from '../primitives/index';
import {FieldWrapper} from './FieldWrapper';
import Checkbox from '../Checkbox';
import RadioGroup from '../Radio/RadioGroup';
import Input from '../Input';
import Text from '../Text';

# FieldWrapper

## Import

`import { FieldWrapper } from 'fannypack'`

## Basic Usage

The `<FieldWrapper>` component is a utility belt that wraps around form elements.

<Playground>
  <FieldWrapper a11yId="username" label="Username">
    <Input />
  </FieldWrapper>
</Playground>

## Descriptions

The description text is placed underneath the label.

<Playground>
  <FieldWrapper a11yId="username" label="Username" description="Required for your fannypack">
    <Input />
  </FieldWrapper>
</Playground>

## Hints

The hint text is placed underneath the field child.

<Playground>
  <FieldWrapper a11yId="username" label="Username" hint="Must be awesome">
    <Input />
  </FieldWrapper>
</Playground>

## Optional fields

To indicate a field as optional, add the `isOptional` prop.

<Playground>
  <FieldWrapper a11yId="username" label="Username" isOptional>
    <Input />
  </FieldWrapper>
</Playground>

## Required fields

Required fields don't show any visual feedback. Displaying 'optional' labels (as above) instead of a red astrix is more descriptive, less fearful, and accessible. However, adding the `isRequired` prop to the field will enable screen readers to identify required fields.

<Playground>
  <FieldWrapper a11yId="username" label="Username" isRequired>
    <Input />
  </FieldWrapper>
</Playground>

## States

<Playground>
  <FieldWrapper a11yId="username" label="Username" validationText="This is an invalid name" state="danger">
    <Input defaultValue="Jake" />
  </FieldWrapper>
  <FieldWrapper a11yId="fannypacks" validationText="Please check the box!" state="primary" marginTop="major-3">
    <Checkbox label="Fannypacks are great" />
  </FieldWrapper>
  <FieldWrapper a11yId="accept" label="Do you accept?" validationText="Nice one!" state="success" marginTop="major-3">
    <RadioGroup defaultValue={true} name="accept" options={[{ label: 'Yes', value: true }, { label: 'No', value: false }]} />
  </FieldWrapper>
</Playground>

## Custom labels

<Playground>
  <FieldWrapper a11yId="username" label={<Text color="primary">Username</Text>}>
    <Input />
  </FieldWrapper>
</Playground>

## Custom descriptions

<Playground>
  <FieldWrapper a11yId="username" label="Username" description={<Text color="primary">Required for your fannypack</Text>}>
    <Input />
  </FieldWrapper>
</Playground>

## Custom hints

<Playground>
  <FieldWrapper a11yId="username" label="Username" hint={<Text color="primary">Must be awesome</Text>}>
    <Input />
  </FieldWrapper>
</Playground>

## Props

<PropsTable component={FieldWrapper} />

## Theming

### Schema

```jsx
{
  FieldWrapper: {
    base: css | Object,
    label: css | Object,
    description: css | Object,
    hint: css | Object,
    optional: css | Object,
    validation: css | Object
  }
}
```
