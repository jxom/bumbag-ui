---
name: Button
route: /elements/button
menu: Elements
---

import { Playground, PropsTable } from 'docz';
import { Button, Buttons } from '../index';

## Import

`import { Button, Buttons } from 'bumbag'`

## Basic Usage

<Playground>
  <Button>Default button</Button>
</Playground>

## Custom elements

Button, by default, is actually wrapped in a `div` element. See more [here](https://reakit.io/components/button).
However, you are able to change it to a custom element using the `as` prop.

<Playground>
  <Buttons>
    <Button>Default</Button>
    <Button as="a" href="#">Anchor</Button>
  </Buttons>
</Playground>

## Colors

Colors can be added to a button with the `color` prop.

<Playground>
  <Buttons>
    <Button>Default</Button>
    <Button state="primary">Primary</Button>
    <Button state="success">Success</Button>
    <Button state="danger">Danger</Button>
    <Button state="warning">Warning</Button>
  </Buttons>
</Playground>

## Loading

To indicate a button is loading, use the `isLoading` prop.

<Playground>
  <Buttons>
    <Button isLoading>Default</Button>
    <Button state="primary" isLoading>Primary</Button>
    <Button state="primary" isLoading type="link">Primary</Button>
    <Button state="success" isLoading type="outlined">Success</Button>
  </Buttons>
</Playground>

## Outlines

An outline can be placed on a button by assigning `"outlined"` to the `type` prop.

<Playground>
  <Buttons>
    <Button state="primary" type="outlined">Primary</Button>
    <Button state="success" type="outlined">Success</Button>
    <Button state="danger" type="outlined">Danger</Button>
  </Buttons>
</Playground>

## Sizes

Buttons are available in four different sizes and can be added with a `size` prop.

<Playground>
  <Buttons>
    <Button size="small">Small</Button>
    <Button>Default</Button>
    <Button size="medium">Medium</Button>
    <Button size="large">Large</Button>
  </Buttons>
</Playground>

## Links

A button can appear as a link with no styling by adding the `isLink` prop.

<Playground>
  <Button state="primary" type="link">Default</Button>
</Playground>


## Disabled

To indicate a button as disabled, use the `disabled` prop.

<Playground>
  <Buttons>
    <Button disabled>Default</Button>
    <Button state="primary" disabled>Primary</Button>
    <Button state="success" disabled>Success</Button>
    <Button state="danger" disabled>Danger</Button>
    <Button state="warning" disabled>Warning</Button>
  </Buttons>
</Playground>

## Lists

To group a set of buttons, use the `<Buttons>` component.

<Playground>
  <Buttons>
    <Button>Previous</Button>
    <Button>Next</Button>
  </Buttons>
  <Buttons isGrouped marginTop="1rem">
    <Button>Previous</Button>
    <Button>Now</Button>
    <Button>Next</Button>
  </Buttons>
</Playground>

### Submit/Cancel buttons

## `<Button>` Props

<PropsTable of={Button} />

## `<Buttons>` Props

<PropsTable of={Buttons} />
