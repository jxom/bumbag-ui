---
name: Getting Started
order: 1
route: /
---


## Prerequisites

To install Boxkit Components, you must have **react@16.x** and **styled-components@3.x** installed as a dependency to your project.

To install these prerequisit packages, run the following command:

```curl
yarn react@latest styled-components@latest
```

## Installing the components

Run the following command to install **Boxkit Components**:

```curl
yarn boxkit
```

## Getting set up

To start using the components, please follow these steps:

1. Wrap your application in a `<ThemeProvider>` which is provided by **Boxkit Components**:

```jsx
import { ThemeProvider } from 'boxkit';

const App = () => (
  <ThemeProvider>
    // ... your app
  </ThemeProvider>
);
```

2. Now you can start using components like so!:

```jsx
import { Button } from 'boxkit';

const MyApp = () => (
  <Button>
    Hello world!
  </Button>
);
```

