---
name: Getting Started
order: 1
route: /
---


## Prerequisites

To install Tracksuit, you must have **react@16.x** and **styled-components@3.x** installed as a dependency to your project.

To install these prerequisit packages, run the following command:

```curl
yarn react@latest styled-components@latest
```

## Installing the components

Run the following command to install **Tracksuit**:

```curl
yarn tracksuit
```

## Getting set up

To start using the components, please follow these steps:

1. Wrap your application in a `<ThemeProvider>` which is provided by **Tracksuit**:

```jsx
import { ThemeProvider } from 'tracksuit';

const App = () => (
  <ThemeProvider>
    // ... your app
  </ThemeProvider>
);
```

2. Now you can start using components like so!:

```jsx
import { Button } from 'tracksuit';

const MyApp = () => (
  <Button>
    Hello world!
  </Button>
);
```

