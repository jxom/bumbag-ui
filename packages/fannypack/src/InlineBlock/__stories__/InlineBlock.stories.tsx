import * as React from 'react';
import { Box, InlineBlock, Text, Heading, Set } from '../../';

export default { title: 'InlineBlock' };

export const _default = () => <InlineBlock>This is an inline block</InlineBlock>;
