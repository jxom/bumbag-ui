import PropTypes from '../../components/PropTypes';
import Theme from '../../components/Theme';
import { Inline } from 'fannypack/Inline/Inline';

# Inline

```js
import { Inline } from 'fannypack';
```

By default it renders a `<Box>` component with `display: inline`.

```jsx-live
<Inline>This is an inline</Inline>
```

# Inline Props

<!-- Automatically generated -->

No props to show.

<details><summary>Uses <code><strong>&#60;Box&#62;</strong></code> props</summary>

### **`use`**

```
string | ComponentClass<any, any> | FunctionComponent<any>
```

### **`className`**

```
string
```

### **`children`**

```
string | number | boolean | {} | ReactElement<any, string | ((props: any) => ReactElement<any, string | ... | (new (props: any) => Component<any, any, any>)>) | (new (props: any) => Component<any, any, any>)> | ReactNodeArray | ReactPortal | ((props: BoxProps) => ReactNode)
```

### **`overrides`**

```
{ base?: any; }
```

### **`showBreakpoint`**

```
string
```

### **`hiddenBreakpoint`**

```
string
```

</details>
        

# Theming

<Theme component={Inline} overrides={['Inline.base']}>This is an inline</Theme>
