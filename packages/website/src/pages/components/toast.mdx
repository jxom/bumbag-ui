import Theme from '../../components/Theme';
import { Toast } from 'fannypack';

# Toasts

## Import

```jsx
import { Toast } from 'fannypack'
```

## Setup

```jsx
import { Provider, ToastManager } from 'fannypack';

const App = () => (
  <Provider theme={theme}>
    // ... your app
    <ToastManager />
  </Provider>
);
```

## Usage

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.add({ title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

### Types

#### Success

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.success({ title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

#### Danger

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.danger({ title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

#### Warning

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.warning({ title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

#### Info

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.info({ title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

### Placement

You can customise the placement of the toasts by using the `placement` prop on `<ToastManager>`.

> `placement` is set to `"top-end"` by default.

```jsx
import { ToastManager } from 'fannypack';

const App = () => (
  <ToastManager placement="bottom" />
);
```

### Stacked toasts

By default, toasts in Fannypack are stacked, meaning that you can display multiple toasts at the one time. However, you have the ability to disable this using the `isStacked` prop on `<ToastManager>`.

```jsx
import { ToastManager } from 'fannypack';

const App = () => (
  <ToastManager isStacked={false} />
);
```

### Custom Toast props

You can override the `Toast` component props using the `Toast.defaultProps` theme variable. This variable accepts an object of the [`<Toast>` props](#props).

```jsx
import { Provider } from 'fannypack';

const theme = {
  Toast: {
    defaultProps: {
      accent: 'bottom',
      hasIcon: false
    }
  }
}

const App = () => (
  <Provider theme={theme}>
    // ... your app
  </Provider>
);
```

Or you can change it via the toast instance:

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.info({ accent: 'bottom', hasIcon: false, title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

### Dismiss timeout

By default, the toast timeout is 5 seconds (5000ms). You can change this via the `Toast.timeout` theme variable.

> To disable the timeout, set `timeout` to `0`.

```jsx
import { Provider } from 'fannypack';

const theme = {
  Toast: {
    timeout: 2000
  }
}

const App = () => (
  <Provider theme={theme}>
    // ... your app
  </Provider>
);
```

Or you can change it via the toast instance:

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.info({ timeout: 2000, title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

### Disable the countdown bar

```jsx
import { Provider } from 'fannypack';

const theme = {
  Toast: {
    showCountdown: false
  }
}

const App = () => (
  <Provider theme={theme}>
    // ... your app
  </Provider>
);
```

Or you can change it via the toast instance:

```function-live
function Example() {
  const toasts = useToasts();

  return (
    <Button
      onClick={() =>
        toasts.info({ showCountdown: false, title: 'Did you know?', message: 'Fannypack is a UI Kit for React' })
      }
    >
      Add toast
    </Button>
  )
}
```

## Props

### ToastManager Props

<!-- Automatically generated -->

**<Code marginRight="major-1">fade</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">isStacked</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">slide</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">spacing</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">overlayProps</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  unstable_system?: any;
  defaultChecked?: boolean;
  defaultValue?: string | number | string[];
  suppressContentEditableWarning?: boolean;
  suppressHydrationWarning?: boolean;
  accessKey?: string;
  ... 792 more ...;
  timingFunction?: string;
}`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">toastProps</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  unstable_system?: any;
  defaultChecked?: boolean;
  defaultValue?: string | number | string[];
  suppressContentEditableWarning?: boolean;
  suppressHydrationWarning?: boolean;
  accessKey?: string;
  ... 779 more ...;
  iconProps?: Pick<...>;
}`}
</Code>

<Box marginBottom="major-4" />

<details><Box use="summary" marginBottom="major-2">Inherits <code><strong>Box</strong></code> props</Box>

**<Code marginRight="major-1">use</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | (ComponentClass<any, any> & { useProps: any; })
  | (FunctionComponent<any> & { useProps: any; })`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">className</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">children</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | number
  | boolean
  | {}
  | ReactElement<any, string
  | ((props: any) => ReactElement<any, string
  | ...
  | (new (props: any) => Component<any, any, any>)>)
  | (new (props: any) => Component<any, any, any>)>
  | ReactNodeArray
  | ReactPortal
  | ((props: BoxProps) => ReactNode)`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">altitude</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">variant</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">showBreakpoint</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">hiddenBreakpoint</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">overrides</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  altitudes?: AltitudesThemeConfig;
  borders?: BordersThemeConfig;
  borderRadii?: BorderRadiiThemeConfig;
  breakpoints?: BreakpointsThemeConfig;
  fontSizes?: FontSizeThemeConfig;
  fontWeights?: FontWeightsThemeConfig;
  ... 80 more ...;
  Template?: TemplateThemeConfig;
}`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">elementRef</Code>** <Code fontSize="150" palette="primary">((instance: any) =&#62; void) | RefObject&#60;any&#62;</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">themeKey</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

</details>
            

### Toast Props

<!-- Automatically generated -->

**<Code marginRight="major-1">countdown</Code>** <Code fontSize="150" palette="primary">number</Code> 

<Box marginBottom="major-4" />

<details><Box use="summary" marginBottom="major-2">Inherits <code><strong>Alert</strong></code> props</Box>

**<Code marginRight="major-1">accent</Code>** <Code fontSize="150" palette="primary">true | &#34;bottom&#34; | &#34;top&#34;</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">accentSize</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">hasIcon</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">isInline</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">onClickClose</Code>** <Code fontSize="150" palette="primary">(event: MouseEvent&#60;any, MouseEvent&#62;) =&#62; void</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">showCloseButton</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">title</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">type</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">closeButtonProps</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  unstable_system?: any;
  defaultChecked?: boolean;
  defaultValue?: string | number | string[];
  suppressContentEditableWarning?: boolean;
  suppressHydrationWarning?: boolean;
  accessKey?: string;
  ... 790 more ...;
  spinnerProps?: SpinnerProps;
}`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">closeButtonIconProps</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  children?: string | number | boolean | {
     | ReactElement<any, string | ((props: any) => ReactElement<any, string | ... | (new (props: any) => Component<any, any, any>)>) | (new (props: any) => Component<any, any, any>)> | ... 47 more ... | (ReactPortal & ((props: BoxProps) => ReactNode));
    ... 775 more ...;
    label?:...`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">iconProps</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  children?: string | number | boolean | {
     | ReactElement<any, string | ((props: any) => ReactElement<any, string | ... | (new (props: any) => Component<any, any, any>)>) | (new (props: any) => Component<any, any, any>)> | ... 47 more ... | (ReactPortal & ((props: BoxProps) => ReactNode));
    ... 775 more ...;
    label?:...`}
</Code>

<Box marginBottom="major-4" />

</details>
            

<details><Box use="summary" marginBottom="major-2">Inherits <code><strong>Box</strong></code> props</Box>

**<Code marginRight="major-1">use</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | (ComponentClass<any, any> & { useProps: any; })
  | (FunctionComponent<any> & { useProps: any; })`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">className</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">children</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | number
  | boolean
  | {}
  | ReactElement<any, string
  | ((props: any) => ReactElement<any, string
  | ...
  | (new (props: any) => Component<any, any, any>)>)
  | (new (props: any) => Component<any, any, any>)>
  | ReactNodeArray
  | ReactPortal
  | ((props: BoxProps) => ReactNode)`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">altitude</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">variant</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">showBreakpoint</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">hiddenBreakpoint</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">overrides</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  altitudes?: AltitudesThemeConfig;
  borders?: BordersThemeConfig;
  borderRadii?: BorderRadiiThemeConfig;
  breakpoints?: BreakpointsThemeConfig;
  fontSizes?: FontSizeThemeConfig;
  fontWeights?: FontWeightsThemeConfig;
  ... 80 more ...;
  Template?: TemplateThemeConfig;
}`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">elementRef</Code>** <Code fontSize="150" palette="primary">((instance: any) =&#62; void) | RefObject&#60;any&#62;</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">themeKey</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

</details>
            

## Theming

<Theme
  component={Toast}
  title="Did you know?"
  overrides={['Toast.css.root', 'Toast.Content.css.root', 'Toast.Title.css.root', 'Toast.Description.css.root', 'Toast.IconWrapper.css.root', 'Toast.CloseButton.css.root']}>
  Fannypack is a React UI Kit.
</Theme>
