import Theme from '../../components/Theme';
import { FieldSet } from 'fannypack/FieldSet';

# FieldSet

## Import

```jsx
import { FieldSet } from 'fannypack'
```

### Usage

The `<FieldSet>` component is a utility belt that wraps around field elements to space them out correctly. It uses the [`<Set>` utility](/utils/set) internally.

```jsx-live
<FieldSet>
  <FieldSet isHorizontal>
    <InputField name="firstName" label="First name" />
    <InputField name="lastName" label="Last name" />
  </FieldSet>
  <InputField name="username" label="Username" />
  <InputField name="password" label="Password" type="password" />
  <Button palette="primary">Submit</Button>
</FieldSet>
```

## Props

### FieldSet Props

<!-- Automatically generated -->

**<Code marginRight="major-1">isHorizontal</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">spacing</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

## Theming

<Theme
  component={FieldSet}
  overrides={[
    'FieldSet.base'
  ]}>
  Hello world
</Theme>
