import Theme from '../../components/Theme';
import { Autosuggest } from 'fannypack';

# Autosuggest

## Import

```jsx
import { Autosuggest } from 'fannypack'
```

## Usage

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Automatic selection

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      automaticSelection
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Restrict to options

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      restrictToOptions
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Loading indicator

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      isLoading
      onChange={setValue}
      options={[]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Custom option component

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      renderOption={({ MatchedLabel, option }) =>
        <React.Fragment>
          <MatchedLabel />
          <br/>
          <Text fontSize="100">{option.price}</Text>
        </React.Fragment>
      }
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples', price: '$10.00/kg' },
        { key: 2, label: 'Bananas', value: 'bananas', price: '$8.50/kg' },
        { key: 3, label: 'Oranges', value: 'oranges', price: '$9.50/kg' },
        { key: 4, label: 'Mangos', value: 'mangos', price: '$8.00/kg' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Custom empty text

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      emptyText="No fruits found."
      restrictToOptions
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Custom empty component

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      renderEmpty={({ create, emptyText, inputValue }) =>
        <Autosuggest.StaticItem>
          {emptyText}
          <br />
          <Link onClick={() => create({ label: inputValue, value: inputValue })}>
            Create <Text fontWeight="semibold">{inputValue}</Text>
          </Link>
        </Autosuggest.StaticItem>
      }
      restrictToOptions
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      renderEmpty={({ create, emptyText, inputValue }) =>
        <Autosuggest.Item
          onClick={() => create({ label: inputValue, value: inputValue })}
        >
          Create <Text fontWeight="semibold">{inputValue}</Text>
        </Autosuggest.Item>
      }
      restrictToOptions
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Option icons

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      restrictToOptions
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples', iconBefore: 'solid-apple-alt' },
        { key: 2, label: 'Bananas', value: 'bananas', iconAfter: 'solid-apple-alt' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Disabled

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      disabled
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas' },
        { key: 3, label: 'Oranges', value: 'oranges' },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Disabled item

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  return (
    <Autosuggest
      onChange={setValue}
      options={[
        { key: 1, label: 'Apples', value: 'apples' },
        { key: 2, label: 'Bananas', value: 'bananas', disabled: true },
        { key: 3, label: 'Oranges', value: 'oranges', disabled: true },
        { key: 4, label: 'Mangos', value: 'mangos' }
      ]}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

### Fetching data (async)

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  const getOptions = React.useCallback(async ({ searchText }) => {
    return fetch(`https://rickandmortyapi.com/api/location/?name=${searchText}`)
      .then(res => res.json())
      .then(({ results }) => ({
        options: results.map(character => ({ key: character.id, label: character.name, value: character }))
      }))
      .catch(err => ({ options: [] }));
  }, [])

  return (
    <Autosuggest
      cacheKey="basic"
      onChange={setValue}
      loadOptions={getOptions}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

#### Pagination

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  const getOptions = React.useCallback(async ({ page, searchText }) => {
    return fetch(`https://rickandmortyapi.com/api/location/?name=${searchText}&page=${page}`)
      .then(res => res.json())
      .then(({ results }) => ({
        options: results.map(character => ({ key: character.id, label: character.name, value: character }))
      }))
      .catch(err => ({ options: [] }));
  }, [])

  return (
    <Autosuggest
      pagination
      cacheKey="pagination"
      onChange={setValue}
      loadOptions={getOptions}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

#### Deferred

```function-live
function Example() {
  const [value, setValue] = React.useState({ label: '' });

  const getOptions = React.useCallback(async ({ searchText }) => {
    return fetch(`https://rickandmortyapi.com/api/location/?name=${searchText}`)
      .then(res => res.json())
      .then(({ results }) => ({
        options: results.map(character => ({ key: character.id, label: character.name, value: character }))
      }))
      .catch(err => ({ options: [] }));
  }, [])

  return (
    <Autosuggest
      defer
      cacheKey="defer"
      onChange={setValue}
      loadOptions={getOptions}
      placeholder="Search for a fruit..."
      value={value}
    />
  )
}
```

## Props

## Theming
