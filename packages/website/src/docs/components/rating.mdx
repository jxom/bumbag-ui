import { Box, Button, Rating } from 'bumbag';
import Theme from '../../components/Theme';

# Rating

The `<Rating>` component displays a set of selectable (or static) rating stars.

## Import

```jsx
import { Rating } from 'bumbag';
```

## Usage

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Rating
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

### Static

Render a set of static unselectable stars with the `isStatic` prop.

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Rating
      isStatic
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

### Disabled

Disable the selectable stars with a `disabled` prop.

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Rating
      disabled
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

### Sizes

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Stack spacing="major-2">
      <Rating
        size="small"
        onChange={value => setValue(value)}
        value={value}
      />
      <Rating
        onChange={value => setValue(value)}
        value={value}
      />
      <Rating
        size="medium"
        onChange={value => setValue(value)}
        value={value}
      />
      <Rating
        size="large"
        onChange={value => setValue(value)}
        value={value}
      />
    </Stack>
  );
}
```

### Max rating

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Rating
      maxValue={10}
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

### Custom items

```function-live
function Example() {
  const [value, setValue] = React.useState(3);
  return (
    <Rating
      item={<Icon icon="solid-igloo" />}
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

```function-live
function Example() {
  const [value, setValue] = React.useState();
  return (
    <Rating
      isSingular
      color="primary"
      items={[
        <Icon icon="solid-frown" />,
        <Icon icon="solid-meh" />,
        <Icon icon="solid-smile" />
      ]}
      size="large"
      onChange={value => setValue(value)}
      value={value}
    />
  );
}
```

## Props

### Rating Props

<!-- Automatically generated -->

**<Code marginRight="major-1">color</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">disabled</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">item</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`ReactElement<any, string
  | ((props: any) => ReactElement<any, string
  | ...
  | (new (props: any) => Component<any, any, any>)>)
  | (new (props: any) => Component<any, any, any>)>`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">items</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`ReactElement<any, string
  | ((props: any) => ReactElement<any, string
  | ...
  | (new (props: any) => Component<any, any, any>)>)
  | (new (props: any) => Component<any, any, any>)>[]`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">isSingular</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">isStatic</Code>** <Code fontSize="150" palette="primary">boolean</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">maxValue</Code>** <Code fontSize="150" palette="primary">number</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">onChange</Code>** <Code fontSize="150" palette="primary">(index: number) =&#62; void</Code> <Text marginLeft="major-1" fontSize="150" textTransform="uppercase" color="gray">Required</Text>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">roverProps</Code>** <Code fontSize="150" palette="primary">{ baseId?: string; stopId?: string; }</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">size</Code>** <Code fontSize="150" palette="primary">&#34;default&#34; | &#34;small&#34; | &#34;medium&#34; | &#34;large&#34;</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">value</Code>** <Code fontSize="150" palette="primary">number | void</Code> <Text marginLeft="major-1" fontSize="150" textTransform="uppercase" color="gray">Required</Text>

<Box marginBottom="major-4" />

<details><Box use="summary" marginBottom="major-2">Inherits <code><strong>Box</strong></code> props</Box>

**<Code marginRight="major-1">use</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | (ComponentClass<any, any> & { useProps: any; })
  | (FunctionComponent<any> & { useProps: any; })`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">className</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">children</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`string
  | number
  | boolean
  | {}
  | ReactElement<any, string
  | ((props: any) => ReactElement<any, string
  | ...
  | (new (props: any) => Component<any, any, any>)>)
  | (new (props: any) => Component<any, any, any>)>
  | ReactNodeArray
  | ReactPortal
  | ((props: BoxProps) => ReactNode)`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">altitude</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">variant</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">colorMode</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">overrides</Code>**  

<Code isBlock palette="primary" fontSize="150" padding="minor-1" marginBottom="major-2">
{`{
  altitudes?: AltitudesThemeConfig;
  borders?: BordersThemeConfig;
  borderRadii?: BorderRadiiThemeConfig;
  breakpoints?: BreakpointsThemeConfig;
  fonts?: FontsThemeConfig;
  fontSizes?: FontSizeThemeConfig;
  ... 84 more ...;
  Template?: TemplateThemeConfig;
}`}
</Code>

<Box marginBottom="major-4" />

**<Code marginRight="major-1">elementRef</Code>** <Code fontSize="150" palette="primary">((instance: any) =&#62; void) | RefObject&#60;any&#62;</Code> 

<Box marginBottom="major-4" />

**<Code marginRight="major-1">themeKey</Code>** <Code fontSize="150" palette="primary">string</Code> 

<Box marginBottom="major-4" />

</details>
            

### Theming

<Theme component={Rating} onChange={() => {}} value={3} overrides={['Rating.css.root', 'Rating.Item.css.root']} />
