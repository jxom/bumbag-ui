import PropsTable from '../../components/PropsTable';
import { Box } from 'fannypack';
import { Code } from 'fannypack/Code/Code';
import { HighlightedCode } from 'fannypack/Code/HighlightedCode';

# Code

### Import

```jsx
import { Code, HighlightedCode } from 'fannypack'
```

### Basic Usage

`<Code>` renders a `<code>` element by default with reset styles and theme styles.

```.jsx
<Code>Hello world!</Code>
```

### Code blocks

A code block can be rendered by setting the `isBlock` prop. This will wrap the code in a `<pre>`.

```.jsx
<Code isBlock>
{`function log(message) {
  console.log(message);
}`}
</Code>
```

### Highlighted code

The `<HighlighedCode>` component uses [Prism](https://prismjs.com/) under the hood. To highlight a language, specify the `lang` prop.

```.jsx
<HighlightedCode lang="javascript">
{`console.log('Hello world')`}
</HighlightedCode>
<HighlightedCode isBlock lang="html" marginTop="major-3">
{`<body>
  <div class="content">Hello world</div>
</body>`}
</HighlightedCode>
```

You can also pass a `showLabel` prop to show a label of the language in the top right corner:

```.jsx
<HighlightedCode isBlock lang="javascript" showLabel>
{`function log(message) {
  console.log(message);
}`}
</HighlightedCode>
```

and show line numbers with `showLineNumbers`:

```.jsx
<HighlightedCode isBlock lang="javascript" showLineNumbers>
{`function log(message) {
  console.log(message);
}`}
</HighlightedCode>
```



### `<Code>` Props

<PropsTable component={Code} />

### `<HighlightedCode>` Props

<PropsTable component={HighlightedCode} />

### Theming

#### Schema

```jsx
{
  Code: {
    base: css | Object,

    // Styles to apply for blocked code
    block: css | Object,

    // Styles to apply for inline code
    inline: css | Object
  }
}
```

[Learn more about theming](/styling#theming)
